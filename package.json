{
  "name": "promotors-site",
  "version": "1.0.0",
  "private": true,
  "description": "Promotors static customer site",
  "scripts": {
    "build": "node scripts/build-static.js",
    "preview": "npx http-server dist -p 4173",
    "deploy:firebase": "npm run build && npx firebase-tools deploy --only hosting",
    "deploy:cloudflare": "npm run build && npx wrangler pages deploy dist --project-name promotors-site"
  },
  "devDependencies": {
    "firebase-tools": "^15.22.4",
    "http-server": "^14.1.1",
    "playwright": "^1.61.1",
    "wrangler": "^4.107.0"
  }
}
